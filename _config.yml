# Site settings
title: Sara L Loo
description: 'Personal website of Sara L Loo, PhD'
domain: saraloo.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: http://saraloo.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: '/'
# google_analytics: 'UA-XXXXXX-X'
# disqus_shortname: 'your-disqus-name'
# disable_landing_page: true

author:
  name: 'Sara L Loo'
  email: sloo2@jhu.edu
  twitter_username: saraliyenloo
  github_username:  saraloo
  linkedin_username:  saraloo93

defaults:
  -
    scope:
      path: ''
      type: 'posts'
    values:
        layout: 'post'

# Build settings
destination: _site
paginate: 10
permalink: /:year/:title/
markdown: kramdown
highlighter: rouge

sass:
    sass_dir: _sass
    style: compressed

kramdown:
  # use Github Flavored Markdown
  input: GFM
  # do not replace newlines by <br>s
  hard_wrap: false

plugins: ['jekyll-paginate']
exclude: ['README.md', 'Gemfile', 'Gemfile.lock', 'screenshot.png']
